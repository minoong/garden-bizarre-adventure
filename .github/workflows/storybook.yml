name: Deploy Storybook to GitHub Pages

on:
  pull_request:
    types:
      - closed
  workflow_dispatch:

jobs:
  deploy-storybook:
    if: |
      github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'pull_request' &&
       github.event.pull_request.merged == true &&
       github.event.pull_request.base.ref == 'main' &&
       contains(github.event.pull_request.labels.*.name, 'storybook'))
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pages: write
      id-token: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js and pnpm
        uses: ./.github/actions/setup-node-pnpm

      - name: Build Storybook
        run: pnpm run build-storybook
        env:
          NEXT_PUBLIC_KAKAO_APP_KEY: ${{ secrets.NEXT_PUBLIC_KAKAO_APP_KEY }}
          NEXT_PUBLIC_BITHUMB_REST_API_URL: ${{ secrets.NEXT_PUBLIC_BITHUMB_REST_API_URL }}
          NEXT_PUBLIC_BITHUMB_WEBSOCKET_API_URL: ${{ secrets.NEXT_PUBLIC_BITHUMB_WEBSOCKET_API_URL }}

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./storybook-static
          publish_branch: gh-pages
